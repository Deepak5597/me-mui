import React from 'react';
import ScrollableWrapper from '../utility/ScrollableWrapper';

export default function dashboard() {
    return (
        <ScrollableWrapper>Dashboard lorem</ScrollableWrapper>
    )
}
